# Agent Browser v0.2.0 — Demo Shot List

**Duration:** 60 seconds (45–75s acceptable)  
**Aspect ratio:** 16:9 primary; export 9:16 cutdown for Shorts/Reels  
**Aesthetic:** Dark terminal (`#1C1C1C`), monospace, white/green text, minimal motion  
**Framework:** Hook → Pain → Workflow → Proof → CTA (Talocode video skill)

| # | Time | Shot | Caption | Audio |
|---|------|------|---------|-------|
| 1 | 0:00–0:03 | Terminal cursor blink | Agents ship code. Who checks the page? | Silence + subtle typing SFX |
| 2 | 0:03–0:08 | Fast montage: failed deploy anxiety (text only) | One-shot checks miss the story | Low tension bed |
| 3 | 0:08–0:12 | `agent-browser --help` scroll | Agent Browser — safe automation for agents | VO intro |
| 4 | 0:12–0:18 | `session create --name deploy-check --json` | Start a logical session | VO |
| 5 | 0:18–0:28 | `check https://example.com --session … --screenshot-out ./deploy.png` | Smoke check + screenshot | VO + real JSON output |
| 6 | 0:28–0:38 | `console` then `network` with same session | Console + network evidence | VO |
| 7 | 0:38–0:48 | `session report … --format markdown` scroll | Markdown report with next action | VO |
| 8 | 0:48–0:54 | Static card: MCP tool names + safety bullets | MCP sessions. Safe by default. | VO |
| 9 | 0:54–1:00 | npm install command + repo URL | Install v0.2.0 today | VO + CTA |

## B-roll (optional)

- `deploy.png` thumbnail (real screenshot from example.com check)
- `.agent-browser/traces/` folder listing (no secrets visible)

## Do not

- Fake terminal output
- Claim live browser persistence across CLI invocations
- Attack competitors
- Use login flows or private URLs

## Recording plan

1. `npx playwright install chromium` (if needed)
2. Run `scripts/demo-v020.sh` in a clean temp directory
3. Record with asciinema or OBS at 1080p
4. Burn captions from `v0.2.0-demo-script.md`
5. Export MP4 manually — **not generated in CI for this release**

## Video file status

**Not generated in-repo** — no Remotion/ffmpeg/ClipLoop tooling in agent-browser. Package is script + shot list + social post only.