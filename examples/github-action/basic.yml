name: Agent Browser basic check

on:
  workflow_dispatch:

jobs:
  browser-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Smoke check public URL
        uses: talocode/agent-browser@v0
        with:
          url: https://example.com